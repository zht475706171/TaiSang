# 2026-09-04 会话日志:Sidebar 布局重排(固定区 + 唯一滚动区)

## 起因

泰哥要求:历史会话挪到下半部分,上半保留配置/新对话,参考 WeKnora(D:/Project/WeKnora)的排布。选定方案 B(WeKnora 吸顶式),另加 Skill 管理 / MCP 管理 两个占位入口 + 吸顶分隔线。

**第二轮调整**:泰哥反馈"现在都是能滚动的",要求新对话 + Skill/MCP 入口固定不动,只有历史会话可滚动。最终结构改为上下分区(方案 A 形态)。

## 最终改动(仅 Sidebar.vue)

- **固定区**:`.new-session-wrap`(新对话,去掉 sticky)+ `.nav-menu`(Skill 管理 code icon / MCP 管理 server icon,WeKnora menu_item 样式 38px + hover,页面未实现点击无动作),nav-menu 底部 border-bottom 分隔线
- **唯一滚动区**:`.session-section`(flex:1 + overflow-y:auto + min-height:0 + Claude 风格细滚动条),内含"历史会话"小节标题 + 会话列表
- **底部**:`.sidebar-footer`(设置)flex-shrink:0 固定
- `.menu-top` 滚动容器(第一轮方案 B 的产物)已删除
- script 逻辑零改动

## 演进过程(两轮)

1. 第一轮:方案 B 吸顶式 —— `.menu-top` 单滚动容器,新对话 sticky 吸顶 + 分隔线,Skill/MCP/历史会话跟着滚
2. 第二轮:泰哥要求固定区 —— 拆掉 `.menu-top`,新对话+入口挪到滚动区外,历史会话独立滚动,分隔线移到固定区/滚动区交界

## 第三轮微调(泰哥验收反馈)

- 删 sidebar 品牌块(TaiSang + coding agent),`.brand` 相关 CSS 全清,`.new-session-wrap` padding-top 调 16px 补偿顶部留白
- EmptyState 副标题"输入问题,Enter 发送" → "你好，我是TaiSang，你的私人agent，有什么需要我帮助的吗"(title"开始对话"保留)
- 第四轮:品牌块恢复但只留 `<t-icon name="robot">` + "TaiSang"(coding agent 副标题不回来),new-session-wrap padding-top 改回 8px
- TDesign 图标是 sprite 模式:`<svg class="t-icon t-icon-robot"><use href="#t-icon-robot">`,验证要看 `symbol#t-icon-robot` 是否存在 + use 尺寸,光查 svg.innerHTML 会误判为空
- 验证:brand/icon/TaiSang 渲染正确、无 tagline、type-check/build 干净

## 第五轮:输入框一体化(泰哥反馈"中间空白是bug吧?")

- 原样式是分离式:textarea(6px 圆角边框)+ gap 8px + TDesign"发送"文字按钮,泰哥觉得中间空白+按钮周边空白像 bug
- 改成 Claude/ChatGPT 一体式:`.input-row` 大圆角容器(14px,1px 边框,padding 8/8/8/16,focus-within 变 brand 色),textarea 无边框透明,发送按钮改 `shape="circle"` 纯图标(去"发送"文字,补 aria-label)
- `.message-input` 去 border-top,padding 改 0 24px 20px;EmptyState 的 deep 覆盖只剩 `padding: 0`
- ChatView 底部输入框(共享组件)同步新样式
- 验证:首页 739x68 一体容器/圆按钮在容器内/发送链路(建会话→SSE→mock 回复→工具卡片)全通,测试会话已删

## Bug 修复:会话删除竞态导致 Errno 2(泰哥线上反馈)

**现象**:泰哥在 `/chat/b78bd5c1` 页面发消息收到 `agent run failed: [Errno 2] No such file or directory: ...\\b78bd5c1\\conversation.jsonl`。根因:我测完用 API 删了 `b78bd5c1`,但 mock agent run 线程还在后台跑,`on_append` 继续写已删目录炸了。

**修复**(两层加固):
1. `SessionRegistry.delete(session_id, timeout=5)`:先 pop 内存(挡新 run)→ `sess.lock.acquire(timeout=5)` 等在跑的 run 结束 → 再 rmtree;超时返回 False
2. `ConversationStore.append()`:写前 `mkdir(parents=True, exist_ok=True)` 自愈,目录被外部删也能恢复
3. `app.py delete` 路由:`delete` 返回 False 时查仍存在 → 409 busy,不存在 → 幂等 200
4. `Sidebar.handleDelete`:try/catch,失败 alert"会话正在运行,请等当前回复结束后再删"

**TDD**:4 个新测试先红后绿(append 自愈 / delete 等锁 / delete 超时 / delete 期间 writer 不崩)
**验证**:192 passed(188 + 4 新)、type-check/build 干净、坑记入 lessons-learned.md

## 第六轮:白角修复 + 新对话懒建会话(泰哥反馈)

- **白角根因**:`.app-main` 灰底(--td-bg-color-page)上,`.message-input` 残留一条全宽白底(旧 border-top 样式时代产物),圆角输入容器外面露出白色矩形角。ChatView 里 chat-body 与 message-input 是并列 flex 不重叠,白底根本不需要 → 直接删 `background`
- **新对话懒建会话**:`App.vue handleNewSession` 原来立刻 `createNew()` 建空 session(无标题僵尸会话)。改成 `router.push('/')` 进首页草稿态;EmptyState 的 `handleEmptySend` 本来就是"发首条消息才 createNew + send",复用
- 验证:message-input 透明/只有 input-row 白色圆角块;点新对话 URL 停 '/'、session 数不变、EmptyState 显示;type-check/build 干净
- 未用真实 LLM 实测发送(避免烧 token + 权限卡片卡 run),EmptyState 发送链路是旧逻辑前面轮次已验证

## 验证(Playwright DOM 断言,不截图)

- 第二轮(最终版):滚 `.session-section` 100px,新对话/nav-menu/footer 三者 top 纹丝不动,历史会话精确上移 100px ✅
- sidebar 内唯一 overflow 滚动容器 = `.session-section`(otherScrollers 为空)✅
- nav-menu 有 border-bottom 分隔线 ✅
- 两轮各造 8/10 个测试会话验证溢出滚动,测完已删干净(剩泰哥 1 个真实会话)✅
- 会话点击跳转 /chat/:id 未被破坏 ✅
- type-check 干净,两次 build 成功 ✅

## 踩坑

- 第一次滚动断言时 URL 神秘跳到 /chat/56f96c35,复现不了,判断为一次性现象(Playwright 快照时机),非 app bug——App.vue/store 都无自动导航逻辑,已排查
- `document.querySelector('.t-icon').className` 是 SVGAnimatedString 不是 string,断言要用 `getAttribute('class')`
- Playwright evaluate 里造会话要带 `Content-Type: application/json` + body `{title}`,裸 POST 会 422/失败

## 第七轮:Skill 接入方案设计(当前)

泰哥要求接入 skill 功能,参考 claude-code(D:/GoProject/claude-code)的实现。

**claude-code 调研**:
- `src/skills/loadSkillsDir.ts`:SKILL.md 目录格式 + frontmatter 字段(name/description/when_to_use/allowed_tools/argument-hint/arguments/version/model/disable-model-invocation/user-invocable/hooks/context:fork/agent/effort/shell/paths)
- `src/tools/SkillTool/SkillTool.ts`:{skill, args} schema,permission 模型(deny/allow/safe-properties),fork vs inline 执行
- `src/tools/SkillTool/prompt.ts`:skill 清单走 attachment(system-reminder),不放在 tool description,1% 上下文预算,每条 ≤250 字符,超限降级
- 渐进披露:tool description 是静态通用文案,清单注入对话,调用时才加载 SKILL.md 全文作为 user 消息

**TaiSang 方案**(已和泰哥确认决策点):
- v1 范围:目录格式 + frontmatter 子集(name/description/when_to_use/allowed_tools)+ 两源加载(用户 `~/.taisang/skills` + 项目 `<repo>/.taisang/skills`,项目覆盖用户)+ SkillTool inline 注入 + 清单注入 SYSTEM_PROMPT 尾部 + 全部自动允许 + allowed_tools 提示不硬拦 + Skill 管理 UI
- 延后:fork / hooks / 条件 paths / 插件 / MCP 源 / argument-hint / deny 规则 / 硬拦截

**决策**:
- 清单注入位置:SYSTEM_PROMPT 尾部(无 attachment 机制)
- SkillTool 注入方式:user 消息注入 SKILL.md 正文 + tool_result 触发 LLM 继续
- 权限:全部自动允许
- allowed_tools:提示 LLM 遵守不硬拦
- 执行模式:只 inline

**Plan 文件**:`docs/superpowers/plans/2026-09-04-skill-integration.md`,8 个 Task,每个 TDD(先红后绿)+ commit

## 状态

- Plan 已写完,等泰哥 review + 选执行方式(subagent-driven / inline)
- 未提交(等泰哥指示)
- 后端 8765 还在跑(PID 50468)

## 第八轮:Skill 接入实施(Subagent-Driven,8 Task 全完成)

泰哥确认方案后选 Subagent-Driven 执行,直接在 main 分支做(泰哥明确同意)。

**7 个实现 commit + 全部 review 通过**:
- `7e177ac` Task1 loader:Skill dataclass + load_skills(user/project 两源,项目覆盖用户,PyYAML 解析 frontmatter,损坏跳过)
- `a5b5106` Task2 SkillTool:inline 注入 SKILL.md 到 ctx 作 user 消息,${TAISANG_SKILL_DIR} 替换,allowed_tools 提示段不硬拦
- `86f33c9` Task3 清单注入:format_skill_listing(每条≤250字,预算2000,极端降级只留名字)注入 SYSTEM_PROMPT 尾部;AgentService 加 skills 参数(向后兼容)
- `d62807a` Task4 ToolRegistry 集成:skills/ctx 参数 + 懒加载 import 防循环引用
- `2ac971f` Task5 加载位置:SkillsConfig(~/.taisang/settings.json skills 字段)+ session_registry._build_session 加载,项目目录自动拼 source_root/.taisang/skills
- `094ccb6` Task6 API:GET /api/skills、POST /reload(no-op,loader 无缓存)、POST /{name}/toggle(持久化 ~/.taisang/skills_state.json,原子写);闭包模式传 source_root
- `1f0eb14` Task7 前端:SkillManage.vue(表格/来源标签/启用开关/重载)+ /skills 路由 + Sidebar 跳转;旧 hash chunk 清理
- `5c3f183` Task8:SPA fallback + e2e 集成测试

**Task 8 发现并修复的预存 bug**:FastAPI 只有 / 回 index.html,/skills、/chat/:id 深链刷新 404(之前 /chat/:id 只在客户端导航下没暴露)。加 catch-all GET fallback(api/static 前缀仍 404),TDD 先红后绿。

**验证**(Playwright DOM 断言,不截图):
- /skills 页表格渲染 test-skill(名称/描述/用户标签/不限制/开关)✅
- toggle 翻转 + API 持久化(disabled=true→false)✅
- Sidebar Skill 管理点击跳 /skills ✅
- e2e 集成测试:脚本化 MockLLM 调 skill → 清单在 system prompt → 工具已注册 → SKILL.md 注入 user 消息(含 allowed_tools 提示 + ${TAISANG_SKILL_DIR} 替换)→ 主循环继续拿最终答案 ✅
- disabled skill 不进清单,调用返回 skill disabled observation ✅
- 221 tests passed(192 基线 + 29 新)、type-check/build 干净
- 测试 skill(~/.taisang/skills/test-skill)已清理

**踩坑**:
- SkillTool/e2e 的 implementer subagent 连续两次 503,Task 7/8 改为自己 inline 做(内容已在 plan 定死,风险低)
- MockLLM 不会主动调工具,e2e 用脚本化 responses([skill_call, final_answer])驱动
- FastAPI 测试里 monkeypatch `taisang.config._settings_path` + `taisang.web.skills_api._STATE_FILE` 隔离真实 ~/.taisang
- Python 3.12 Path.write_text 不自动建父目录,测试要 _write helper 先 mkdir

**架构备忘**(后续加功能参考):
- skill 调用是"注入式":SkillTool.run() 把 SKILL.md 写进 ctx(append_user)+ tool_result 只回 {ok,injected},LLM 下一轮看到注入内容继续
- v1 权限全部自动允许(泰哥定);fork/hooks/条件 paths/插件源/MCP 源/deny 规则/硬拦截全部延后
- SKILL.md 支持的 frontmatter:name/description/when_to_use/allowed_tools,其余字段静默忽略

## 状态(最终)

- 10 commits 在 main(7e177ac..061a7ea,含 plan/memory 文档 4db4c4f + 终审修复 061a7ea),未 push(等泰哥)
- 后端 8765 跑着 mock LLM(新代码,含 skills 路由 + SPA fallback)
- 用户级 skill 目录 ~/.taisang/skills/ 现在为空,放 <name>/SKILL.md 即被加载

## 第九轮:终审修复(2 个 Important)

整体 code review 发现两个跨模块问题(单测都测不出来,MockLLM 不会触发 provider 400):

1. **消息序协议违规**:SkillTool 在 registry.call() 里直接 `ctx.append_user()`,
   导致消息序 assistant(tool_calls) → user → tool。OpenAI 协议要求 assistant 带
   tool_calls 后必须紧跟 tool 消息,严格 provider(如 OpenAI 官方)会 400。
   **修复**:pending 队列延迟注入 —— run() 只入队,主循环在本轮全部
   tool_result append 完后调 `registry.flush_skill_injections()` 统一
   append_user(service.py tool_calls for 循环后、session_memory 触发前)。
   e2e 测试加消息序断言 `idx_assistant_tc < idx_tool < idx_injected_user` 先红后绿。
2. **disabled 开关不传导 agent**:session_registry._build_session 自己 load_skills,
   不读 skills_state.json → UI 关掉的 skill 在新会话 agent 里照样可用。
   **修复**:skills_api `_load_all_skills` 提为公开 `load_skills_with_state(source_root)`,
   API 路由和 _build_session 共用。TDD:写 state {"commit": true} 后建 session,
   断言 agent.skills 里 commit.disabled is True 且不进 system prompt 清单。

顺手:删 skill_tool.py 未用的 `from pathlib import Path`(审查 minor 项)。

- 223 tests passed(221 + 2 新:pending 滞留测试 + disabled 传导测试)
- commit `061a7ea`

**v2 备忘(记录不修)**:CLI 入口没接 skills;同名 skill 项目覆盖用户不可见;
skill 改动要新会话才生效(实例创建时加载);favicon 走 fallback 返回 HTML(无害)。

## 第十轮:Skill 前端导入 + delete + 内置 skills

泰哥问"前端不能直接导入 skill 吗",确认要做。先把决策点对齐再出 plan:

**决策**(泰哥确认):
1. zip 解压到 `~/.taisang/skills/<name>/` 目录形式;单 MD 也建目录(统一)
2. 同名冲突 → 409 → 前端确认 → `?overwrite=true` 覆盖
3. 校验:frontmatter 必须有合法 name(`/^[A-Za-z0-9][A-Za-z0-9_-]*$/` 防目录穿越)、zip-slip 拦截、10MB 上限、SKILL.md 必须在 zip 根或唯一一级目录下
4. source 只分 user/project/system;包内默认带 commit、review 两个内置
5. 支持 delete(内置不可删,顺带清 disabled state 记录)
6. SkillManage 右上角"导入 Skill"按钮

**5 个 commit + 全部 review 通过**:
- `3fbd6a2` Task1 loader:`system_dirs` 形参 + 优先级 project > user > system;包内 `src/taisang/skills/builtin/{commit,review}/SKILL.md`
- `adaed4d` Task1 fix:review 抓到 wheel 不带 builtin/ 的 bug,加 `pyproject.toml` `[tool.setuptools.package-data] taisang.skills = ["builtin/**/*.md"]`,验证 wheel 包含两个文件
- `73a6045` Task2 importer:`import_skill_md/zip(data, skills_root, overwrite)`,`SkillImportError`/`SkillExistsError` 异常体系,15 个测试覆盖所有校验路径
- `260e839` Task3 API:`POST /api/skills/import`(10MB 上限 + 三种文件类型分发 + 异常→400/409 映射)、`DELETE /api/skills/{name}`(`source==system` 拒删 + `dir_path.parent` 必须在可管理 roots + 清理 state),7 个测试
- `af3ce81` Task4 前端:SkillManage 加 import button + hidden file input + DialogPlugin 覆盖确认 + 行内删除按钮 + system 用"内置"标签(warning 色)+ 操作列 system 行显示 `-`;stale hash chunk 清理 6 个(用 `__vite__mapDeps` 反查引用集合,删除孤儿)

**验证**:
- 248 tests passed(基线 223 + 新增 25 = 15 importer + 7 API + 3 loader)
- type-check / build 干净
- Playwright DOM 断言全过:内置行 source 标签"内置"、op 列显示"-"(无删除按钮);上传 smoke-skill.md 出现新行(用户标签 + 删除按钮);删除后行消失
- 后端 8765 重启用新代码,API 返回内置 + 用户 skill

**踩坑**:
- 我数错测试数:plan 写 17 个 importer 测试,实际 15 个,implementer 正确指正(我没核过 test count)
- subagent 起的 backend 进程会随 subagent 死亡,后续 restart 必须用我自己的 run_in_background(本轮 8765 死掉 2 次,implementer 杀一次 + subagent 销毁一次,都需手动重起)
- TDesign 全局组件(经 `app.use(TDesign)`)vs 按需 plugin API(`MessagePlugin`/`DialogPlugin`)分清:前者模板里直接 `<t-table>`,后者 `import { ... }` 拿到调用句柄

**架构备忘**:
- skill 导入是"软落盘":前端确认 → `?overwrite=true` 重发 → 后端走 `import_skill_*` 校验全过 → rmtree 旧版(只在 rmtree 之前完成所有校验)→ 解压新版。`overwrite=False + 存在` 抛 `SkillExistsError` 走 409 让前端再问
- 安全:frontmatter name 字符集 + zip-slip + dir_path.parent 二次校验三道墙,任何一道都足以独立阻断
- delete 不删 system + 不删非 managed roots + 清 state 记录(re-import 不会莫名 disabled)
- 优先级:project > user > system;用户可覆盖内置(via 同名 user/project skill)

**状态**:5 commits(3fbd6a2..af3ce81)在 main,未 push(等泰哥)。8765 跑着新代码。Plan 文档 `docs/superpowers/plans/2026-09-04-skill-import.md` 未提交(随本 memory 一起)。