# 键盘快捷键 + 可访问性实施计划(Plan 8)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 全局键盘快捷键(Cmd/Ctrl+K 新对话、Cmd/Ctrl+, 配置)+ Esc 关闭配置 + 输入框自动聚焦 + ARIA 标签。

**Architecture:** App.vue 加全局 keydown 监听,handleNewSession 提取到 App.vue;MessageInput 加 autofocus prop;各组件加 aria-label。

**Spec:** `docs/superpowers/specs/2026-09-03-keyboard-a11y-design.md`

---

### Task 1: App.vue 全局键盘监听 + handleNewSession 提取

**Files:**
- Modify: `src/taisang/web/frontend/src/App.vue`
- Modify: `src/taisang/web/frontend/src/components/Sidebar.vue`

- [ ] **Step 1: App.vue 加全局键盘监听 + handleNewSession**

`src/taisang/web/frontend/src/App.vue`:
```vue
<template>
  <div class="app-layout">
    <Sidebar @open-config="configOpen = true" @new-session="handleNewSession" />
    <main class="app-main">
      <router-view />
    </main>
    <ConfigModal v-model="configOpen" />
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'
import { useRouter } from 'vue-router'
import Sidebar from '@/components/Sidebar.vue'
import ConfigModal from '@/components/ConfigModal.vue'
import { useSessionStore } from '@/stores/session'

const configOpen = ref(false)
const store = useSessionStore()
const router = useRouter()

async function handleNewSession() {
  const id = await store.createNew()
  store.select(id)
  router.push(`/chat/${id}`)
}

function handleGlobalKeydown(e: KeyboardEvent) {
  // Cmd/Ctrl+K → 新对话
  if ((e.metaKey || e.ctrlKey) && (e.key === 'k' || e.key === 'K')) {
    e.preventDefault()
    handleNewSession()
  }
  // Cmd/Ctrl+, → 打开配置
  if ((e.metaKey || e.ctrlKey) && e.key === ',') {
    e.preventDefault()
    configOpen.value = true
  }
}

onMounted(() => {
  window.addEventListener('keydown', handleGlobalKeydown)
  store.fetchSessions()
})
onUnmounted(() => {
  window.removeEventListener('keydown', handleGlobalKeydown)
})
</script>

<style>
.app-layout {
  display: flex;
  height: 100vh;
  width: 100vw;
  overflow: hidden;
}
.app-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-width: 0;
  background: var(--td-bg-color-page);
}
</style>
```

- [ ] **Step 2: Sidebar 改成 emit 'new-session' + 加 ARIA**

`src/taisang/web/frontend/src/components/Sidebar.vue` template 改:
```vue
<template>
  <aside class="sidebar">
    <div class="brand">
      <div class="brand-mark">
        <span class="diamond">◆</span>TaiSang
      </div>
      <div class="brand-tag">coding agent</div>
    </div>

    <div class="new-session-wrap">
      <t-button
        block
        theme="primary"
        aria-label="新建对话(Cmd/Ctrl+K)"
        @click="emit('new-session')"
      >
        <template #icon>
          <t-icon name="add" />
        </template>
        新对话
      </t-button>
    </div>

    <div class="session-list">
      <SessionItem
        v-for="s in store.sessions"
        :key="s.id"
        :session="s"
        :active="s.id === store.currentId"
        @select="handleSelect(s.id)"
        @delete="handleDelete(s.id)"
      />
      <div v-if="store.sessions.length === 0" class="empty-list">
        暂无会话
      </div>
    </div>

    <div class="sidebar-footer">
      <t-button
        block
        variant="text"
        aria-label="LLM 配置(Cmd/Ctrl+,)"
        @click="emit('open-config')"
      >
        <template #icon>
          <t-icon name="setting" />
        </template>
        设置
      </t-button>
    </div>
  </aside>
</template>

<script setup lang="ts">
import { useRouter } from 'vue-router'
import SessionItem from './SessionItem.vue'
import { useSessionStore } from '@/stores/session'

const emit = defineEmits<{ 'open-config': []; 'new-session': [] }>()
const router = useRouter()
const store = useSessionStore()

function handleSelect(id: string) {
  store.select(id)
  router.push(`/chat/${id}`)
}

async function handleDelete(id: string) {
  if (!confirm('确认删除该会话?')) return
  await store.remove(id)
  if (store.currentId === null) {
    router.push('/')
  }
}
</script>
```

(删掉 `handleNewSession` 函数,改成 emit)

- [ ] **Step 3: type-check**

Run: `cd src/taisang/web/frontend && npm run type-check`
Expected: 无错误

---

### Task 2: MessageInput autofocus + aria-label

**Files:**
- Modify: `src/taisang/web/frontend/src/components/MessageInput.vue`

- [ ] **Step 1: 看 MessageInput 当前实现**

(已读过,直接改)

- [ ] **Step 2: 加 autofocus + aria-label**

`src/taisang/web/frontend/src/components/MessageInput.vue` 的 `<textarea>` 改:
```vue
<textarea
  ref="textareaRef"
  v-model="text"
  class="message-input"
  :placeholder="placeholder"
  aria-label="输入消息,Enter 发送,Shift+Enter 换行"
  @keydown.enter.exact.prevent="handleSend"
  @keydown.shift.enter="handleShiftEnter"
></textarea>
```

加 `defineExpose` 暴露 focus 方法,或在 onMounted 自动 focus。

加 props `autofocus: boolean = false`:
```typescript
const props = defineProps<{ placeholder?: string; autofocus?: boolean }>()
const textareaRef = ref<HTMLTextAreaElement | null>(null)

onMounted(() => {
  if (props.autofocus && textareaRef.value) {
    textareaRef.value.focus()
  }
})
```

- [ ] **Step 3: EmptyState 和 ChatView 的 MessageInput 传 autofocus**

EmptyState.vue 的 MessageInput 加 `autofocus`:
```vue
<MessageInput placeholder="输入问题,Enter 发送..." autofocus @send="handleSend" />
```

ChatView.vue 的 MessageInput 加 `autofocus`:
```vue
<MessageInput v-if="currentSession && messages.length" autofocus @send="handleSend" />
```

- [ ] **Step 4: type-check**

Run: `cd src/taisang/web/frontend && npm run type-check`
Expected: 无错误

---

### Task 3: SessionItem + ChatView topbar aria-label

**Files:**
- Modify: `src/taisang/web/frontend/src/components/SessionItem.vue`
- Modify: `src/taisang/web/frontend/src/views/ChatView.vue`

- [ ] **Step 1: SessionItem 加 aria-label**

SessionItem 的根元素加 `:aria-label`:
```vue
<div
  class="session-item"
  :class="{ active }"
  role="button"
  tabindex="0"
  :aria-label="`会话: ${session.title || session.id}`"
  @click="$emit('select')"
  @keydown.enter.prevent="$emit('select')"
>
```

- [ ] **Step 2: ChatView topbar 按钮 aria-label**

```vue
<t-button variant="text" size="small" aria-label="重置会话(/reset)" @click="handleReset">/reset</t-button>
<t-button variant="text" size="small" aria-label="切换 debug 模式(/debug)" @click="handleDebug">/debug</t-button>
```

- [ ] **Step 3: type-check + build**

Run: `cd src/taisang/web/frontend && npm run type-check && npm run build`
Expected: 无错误,build 成功

- [ ] **Step 4: Python 测试回归**

Run: `python -m pytest --tb=short -q`
Expected: 188 passed

- [ ] **Step 5: Commit**

```bash
git add src/taisang/web/frontend/src/App.vue src/taisang/web/frontend/src/components/ src/taisang/web/frontend/src/views/ChatView.vue src/taisang/web/static/index.html src/taisang/web/static/assets/
git commit -m "feat(frontend): Plan 8 — 键盘快捷键 + 可访问性"
```

---

## 自检 checklist

1. **Spec 覆盖**:
   - ✅ Cmd/Ctrl+K → Task 1
   - ✅ Cmd/Ctrl+, → Task 1
   - ✅ Esc 关闭 ConfigModal → TDesign 自带,Plan 9 e2e 验证
   - ✅ autofocus → Task 2
   - ✅ ARIA 标签 → Task 1/2/3
2. **Placeholder 扫描**:无 TBD
3. **类型一致性**:`emit('new-session')` 在 App.vue 和 Sidebar 一致